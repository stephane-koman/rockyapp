export enum EActionType {
  READ = "read",
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}

export enum ETableActionType {
  BUTTON = "button",
  DROPDOWN = "dropdown"
}

export enum EAgainType {
  Un = "un",
  Une = "une",
}